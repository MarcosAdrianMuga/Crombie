function AreaCirculo(r) {
    return Math.PI * r * r;
}

function SegYUltimoProf(arr) {
    return arr[1], arr[arr.length - 1];
}

const alumno = {
    nombre: "yo",
    apellido: "pepe",
    lenguajePro: "js",
    edad: 111111111111
}

function verObj(obj, que) {
    return console.log(obj[que]); 
}
const hola = 2
console.log(`mi num es ${hola}`); 